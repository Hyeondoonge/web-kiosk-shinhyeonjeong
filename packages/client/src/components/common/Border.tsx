import theme from 'style/theme'
import styled from 'styled-components'

const StyledBorder = styled.div`
  width: 100%;
  height: 1px;
  background-color: ${theme.palette.line};
`

export default StyledBorder
